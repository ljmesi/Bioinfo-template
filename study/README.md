# Directory contents

This directory contains the practical analyses performed in the project. For each `.Rmd` source file there are both converted `.html` and `.pdf` versions available in the `/Rmds/output/` directory.

Directory contains also:

- `bibliography.bib`: all references used in Rmarkdown files in BibTex-format.